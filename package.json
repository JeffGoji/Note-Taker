{
  "dependencies": {
    "express": "^4.17.1",
    "nodemon": "^2.0.14"
  },
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "ng build --prod",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
